export interface Dictionary {
  home: {
    // ... (mantener el contenido existente)
  };
  summary: {
    title: string;
    pageTitle: string;
    summaryTitle: string;
    defaultTitle: string;
    videoIdLabel: string;
    watchOnYoutube: string;
    backToHome: string;
    notFound: string;
    returnHome: string;
    loadingMessage: string;
    errorMessage: string;
  };
  // ... (mantener otras secciones)
}